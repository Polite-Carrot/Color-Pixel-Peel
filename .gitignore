node_modules/
coverage/

# Assembled by sync-web.js for the native shells, from files already
# committed at the root. app/ is NOT ignored: the built bundle is the
# shipped artifact, because Pages serves this branch's root directly.
www/
*.local
.DS_Store
*.log
npm-debug.log*

# Capacitor: the native projects themselves are meant to be committed
# (they hold signing config, icons and plugin wiring), but their build
# output and fetched dependencies are not.
ios/App/Pods/
ios/App/App/public/
ios/App/output/
ios/DerivedData/
android/.gradle/
android/build/
android/app/build/
android/app/src/main/assets/public/
android/local.properties
android/.idea/
